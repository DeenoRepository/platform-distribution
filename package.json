{
  "name": "@platform/distribution",
  "version": "1.0.0",
  "description": "Production Docker distribution and database migration orchestrator",
  "scripts": {
    "test": "vitest run --coverage",
    "migrate:data": "node scripts/migrate-monolith.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.0.5"
  }
}
